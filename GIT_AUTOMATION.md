# 🛠 Моя Git-Мастерская

> **Проект:** Project1  
> **Автор:** Алексей  
> **Инструменты:** PowerShell + Git + Gemini AI

---

## 🚀 Быстрые команды (Automation)

| Команда | Описание | Что делает внутри |
| :--- | :--- | :--- |
| `gcap "текст"` | **Супер-пуш** | `add` + `commit` + `push` |
| `git st` | **Статус** | Проверка измененных файлов |
| `git lg` | **Лог-граф** | Визуальное дерево истории |

---

## 📝 Как пользоваться `gcap`

Эта функция объявлена в моем `$PROFILE` PowerShell.

1. Если нужно сохранить всё быстро:
   ```powershell
   gcap

Весь диалог с Джемини:

Но сначала, давайте, обсудим один вопрос, Вы сказали, что после завершения сеанса виндос, надо заново делать какие-то настройки, чтобы виндос не капризничал, когда я работаю с ПауэрШелл.
А нельзя ли сделать настройку НАВСЕГДА, чтобы не возвращаться каждый раз и не искать решение проблемы?

Отличный вопрос! И у меня есть хорошая новость: мы уже это сделали, но давайте я поясню, чтобы у вас была полная уверенность.

Настройка, которую мы провели, состоит из двух частей, и обе они — «навсегда» (точнее, до тех пор, пока вы не переустановите Windows или не удалите эти файлы).

Вот почему вам не придется ничего искать заново:

1. Файл профиля ($PROFILE) — это автозапуск
Тот файл, куда мы записали функцию gcap, — это не временный блокнот. Это специальный сценарий, который PowerShell выполняет автоматически каждый раз, когда вы открываете терминал.

Как только вы запускаете VS Code и открываете консоль, PowerShell «читает» этот файл и сразу вспоминает, что такое gcap. Вам не нужно вводить код функции снова.

2. Политика выполнения (Execution Policy) — вот тут нюанс
По умолчанию Windows в целях безопасности блокирует запуск любых сценариев (включая ваш профиль). Если вы закроете VS Code, перезагрузите компьютер и завтра откроете терминал, вы можете увидеть красный текст с ошибкой.

Чтобы решить это РАЗ И НАВСЕГДА, выполните в терминале вот эту команду (один раз в жизни):

PowerShell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Что она делает:

Она говорит Windows: «Я доверяю сценариям, которые написаны на этом компьютере (включая мой профиль)».

Флаг -Scope CurrentUser применяет это правило только для вашего пользователя, что безопасно и не требует прав администратора для всей системы.

Подведем итог «вечности» настроек:
Алиасы git st и git lg: Сохранены в глобальном конфиге Git. Будут работать всегда.

Функция gcap: Сохранена в файле профиля. Будет загружаться сама при каждом открытии терминала.

Разрешение на запуск: Команда выше снимает блокировку навсегда.

Если завтра вы откроете VS Code и просто введете gcap, всё должно сработать мгновенно!

Теперь к вашему файлу GIT_AUTOMATION.md! 🎨
Давайте сделаем его по-настоящему красивым, чтобы GitHub отображал его как профессиональную документацию.

Скопируйте этот текст в ваш файл:

Markdown
# 🛠 Моя Git-Мастерская

> **Проект:** Project1  
> **Автор:** Алексей  
> **Инструменты:** PowerShell + Git + Gemini AI

---

## 🚀 Быстрые команды (Automation)

| Команда | Описание | Что делает внутри |
| :--- | :--- | :--- |
| `gcap "текст"` | **Супер-пуш** | `add` + `commit` + `push` |
| `git st` | **Статус** | Проверка измененных файлов |
| `git lg` | **Лог-граф** | Визуальное дерево истории |

---

## 📝 Как пользоваться `gcap`

Эта функция объявлена в моем `$PROFILE` PowerShell.

1. Если нужно сохранить всё быстро:
   ```powershell
   gcap
Если нужно добавить осмысленный комментарий:

PowerShell
gcap "Обновил логику парсера"
⚙️ Технические детали
Для работы функции на новых ветках включена настройка:
git config --global push.autoSetupRemote true

Настроено совместно с Джемини. 15.02.2026


**Как вам такой вариант оформления?** Если нравится, вставляйте в файл, сохраняйте и запускайте вашу новую команду: `gcap "Добавил оформленную шпаргалку"`. 

Жду подтверждения, что файл улетел на GitHub! 🚀

📑 Отчет по настройке автоматизации Git (Windows PowerShell)
Дата: 15 февраля 2026 г.
Среда: Windows 10/11, VS Code Terminal (PowerShell)

🚀 1. Команда «Все в одном»: gcap
Эта функция позволяет выполнить полный цикл отправки изменений на сервер одной короткой командой.

Где настроено: Файл профиля PowerShell ($PROFILE).

Структура команды:
PowerShell
function gcap {
    param([string]$m = "quick update") # $m — это переменная для вашего комментария
    git add .                          # 1. Подготовка всех измененных файлов
    git commit -m $m                   # 2. Создание коммита с вашим текстом
    git push                           # 3. Отправка ветки на GitHub
}
Как использовать:
С вашим комментарием:
gcap "добавил новую функцию" — Git создаст коммит именно с этим текстом.

Без комментария:
gcap — автоматически подставится текст "quick update".

📊 2. Быстрые алиасы Git (внутренние)
Эти команды прописаны в глобальном конфиге Git и всегда начинаются со слова git.

git st (Status)
Что делает: Выводит текущее состояние репозитория (какие файлы изменены, на какой вы ветке).

Зачем: Чтобы быстро понять, что готово к коммиту.

git lg (Log Graph)
Что делает: Рисует красивое дерево истории коммитов с цветами, именами авторов и датами.

Параметры: --graph --abbrev-commit --decorate --all.

Зачем: Позволяет наглядно видеть структуру веток и историю изменений, не заходя на сайт GitHub.

🛠 3. Технические настройки (автоматизация веток)
Чтобы команда gcap работала на любой новой ветке без ошибок, мы применили следующую настройку:

PowerShell
git config --global push.autoSetupRemote true
Эффект: При первом пуше новой ветки Git сам создаст её аналог на GitHub и свяжет их. Вам больше не нужно писать --set-upstream.

📂 4. Как восстановить или изменить эти настройки
Для редактирования gcap:
Введите в терминале code $PROFILE. После изменений сохраните файл и введите . $PROFILE для обновления.

Для просмотра всех алиасов Git:
Введите git config --get-regexp alias.

Этот отчет — ваша надежная шпаргалка. Если возникнут вопросы по любому из пунктов, я всегда готов пояснить!